export type VfxCurve='linear'|'ease-in'|'ease-out'|'smoothstep';
export interface VfxMapping {readonly inputMin:number;readonly inputMax:number;readonly outputMin:number;readonly outputMax:number;readonly curve:VfxCurve;readonly invert:boolean;readonly quantizeSteps:number}
export function mapVfxValue(input:number,mapping:VfxMapping):number{if(!Number.isFinite(input)||!Object.values(mapping).every((v)=>typeof v!=='number'||Number.isFinite(v)))throw new Error('VFX mapping requires finite values');if(mapping.inputMax===mapping.inputMin)throw new Error('VFX mapping input range cannot be zero');let t=Math.max(0,Math.min(1,(input-mapping.inputMin)/(mapping.inputMax-mapping.inputMin)));if(mapping.invert)t=1-t;if(mapping.quantizeSteps>1)t=Math.round(t*(mapping.quantizeSteps-1))/(mapping.quantizeSteps-1);t=mapping.curve==='ease-in'?t*t:mapping.curve==='ease-out'?1-(1-t)*(1-t):mapping.curve==='smoothstep'?t*t*(3-2*t):t;return mapping.outputMin+t*(mapping.outputMax-mapping.outputMin)}
