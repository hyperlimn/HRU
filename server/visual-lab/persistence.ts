import {mkdir,readFile,rename,rm,writeFile} from 'node:fs/promises';import {dirname} from 'node:path';import {z} from 'zod';
import type {VisualConfiguration,VisualProfile} from '../../src/visual-lab/types';import {normalizeVisualConfiguration,VISUAL_SCHEMA_VERSION,visualRegistry} from '../../src/visual-lab/registry';
const value=z.union([z.number().finite(),z.boolean(),z.string(),z.tuple([z.number().finite(),z.number().finite(),z.number().finite()])]);
const profile=z.object({formatVersion:z.literal(1),name:z.string().min(1).max(80),schemaVersion:z.literal(VISUAL_SCHEMA_VERSION),values:z.record(z.string(),value),description:z.string().max(500).optional(),builtIn:z.literal(false),metadata:z.object({createdAt:z.string().optional(),updatedAt:z.string().optional()}).optional()}).strict();
const fileSchema=z.object({formatVersion:z.literal(1),schemaVersion:z.literal(VISUAL_SCHEMA_VERSION),values:z.record(z.string(),value),activeProfile:z.string(),profiles:z.array(profile),favorites:z.array(z.string()),showAdvanced:z.boolean(),favoritesOnly:z.boolean(),ab:z.object({active:z.enum(['A','B']).optional(),A:z.record(z.string(),value).optional(),B:z.record(z.string(),value).optional()}).strict()}).strict();
export interface PersistedVisualLab{values:VisualConfiguration;activeProfile:string;profiles:VisualProfile[];favorites:string[];showAdvanced:boolean;favoritesOnly:boolean;ab:{active?:'A'|'B';A?:VisualConfiguration;B?:VisualConfiguration}}
export class VisualLabPersistence{constructor(private readonly path:string){}async load():Promise<PersistedVisualLab|undefined>{let raw:string;try{raw=await readFile(this.path,'utf8')}catch(error){if((error as NodeJS.ErrnoException).code==='ENOENT')return undefined;return undefined}try{const parsed=fileSchema.parse(JSON.parse(raw));for(const id of parsed.favorites)visualRegistry.get(id);return{...parsed,values:normalizeVisualConfiguration(parsed.values),profiles:parsed.profiles.map((item)=>({...item,values:normalizeVisualConfiguration(item.values)})),ab:{...parsed.ab,...(parsed.ab.A?{A:normalizeVisualConfiguration(parsed.ab.A)}:{}),...(parsed.ab.B?{B:normalizeVisualConfiguration(parsed.ab.B)}:{})}}}catch{return undefined}}async save(data:PersistedVisualLab):Promise<void>{await mkdir(dirname(this.path),{recursive:true});const payload={formatVersion:1,schemaVersion:VISUAL_SCHEMA_VERSION,...data};fileSchema.parse(payload);const temporary=`${this.path}.tmp`;try{await writeFile(temporary,`${JSON.stringify(payload,null,2)}\n`,'utf8');await rename(temporary,this.path)}catch(error){await rm(temporary,{force:true}).catch(()=>undefined);throw error}}}
